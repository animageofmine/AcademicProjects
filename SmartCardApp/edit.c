#include "header.h"

//	static char gszDefEditor[]="vi";
//	static char gszFileToEdit[]="/home/scf-12/csci530/public/hw1/yesterday.txt";
	
int edit(char *sopath,char *filename)
{
//	char cmd[256],*psz=NULL;
	
	/*signal(SIGCHLD,SIG_IGN);
	psz=getenv("EDITOR");
	snprintf(cmd,sizeof(cmd), %s %s",(psz==NULL) ? gszDefEditor : psz, gszFileToEdit);
	system(cmd);*/
	
	return 0;
}
	



















